# Install as /home/project/.env with mode 0600. Replace every
# placeholder locally; never commit the resulting file.
DATABASE_URL=postgresql+psycopg://<worker-role>:<password>@<host>:5432/<database>
RAW_DATA_ROOT=/var/lib/market-data-center/raw
DAILY_BAR_WRITE_BATCH_SIZE=100
SCHEDULER_STORE_PATH=/var/lib/market-data-center/scheduler/jobs.sqlite
PYTDX_POOL_PATH=/var/lib/market-data-center/pytdx_pool.json
PYTDX_VIPDOC_PATH=
PYTDX_DAILY_BAR_TIMEOUT_SECONDS=3
PYTDX_DAILY_BAR_MAX_ATTEMPTS=2
PYTDX_DAILY_BAR_PAGE_SIZE=800
PYTDX_DAILY_BAR_MAX_PAGES=16
TUSHARE_TOKEN=<server-only-token>
WORKER_ADMIN_PORT=8765
# The controlled Worker catalog owns all task times. Environment configuration
# may only enable or disable these optional tasks.
EOD_QUOTE_SNAPSHOT_ENABLED=true
CALL_AUCTION_SNAPSHOT_ENABLED=true
CALL_AUCTION_MARKET_SERIES_ENABLED=true
# Keep disabled until Eastmoney source-rights review is recorded. The controlled
# Worker catalog slot is weekdays at 20:30 Asia/Shanghai.
TRADING_BILLBOARD_ENABLED=false
# Enable only after migration 20260811000500 is applied and AKShare/Eastmoney
# outbound connectivity has passed preflight.
TODAY_LIMIT_UP_SNAPSHOT_ENABLED=false
TODAY_LIMIT_UP_TIMEOUT_SECONDS=10
TODAY_LIMIT_UP_MAX_ATTEMPTS=2
REGULATION_DAILY_ENABLED=false
