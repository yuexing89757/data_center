# 集合竞价五档采集运行手册

任务 `opening-auction-limit-up-quotes` 默认启用。它在交易日 09:15 启动一个会话，
内部每 30 秒采样，至 09:25 最终一轮；每只股票单独请求，不会创建多个 APScheduler job。

## 启用前预检

先应用 migration 到隔离环境，并在 Worker 进程环境中配置非空
`PYSNOWBALL_TOKEN`。只检查 Secret 是否存在，不输出其内容：

```bash
test -n "$PYSNOWBALL_TOKEN" && echo PYSNOWBALL_TOKEN=set
```

数据依赖的只读预检不连接行情服务器，也不写数据库：

```bash
uv run market-data-center auction-quotes-preflight --trade-date 2026-08-03
```

报告必须确认交易日、当日精确 ready 涨停池、成员数和期望 21 轮。禁止回退旧股票池。

## 下一交易日 live gate

在真实 09:15–09:25 窗口核对沪深代码映射、一至五档价格、挂单量的“股”单位、
09:20 阶段边界、每次只请求 1 只和单轮耗时低于 30 秒。单股请求失败时
应继续后续股票，不回退 PYTDX，也不对该股票在同轮重试。
同时确认集合竞价返回档位是否具备标准连续五档语义。未通过前，任务保持
`AUCTION_COLLECTION_ENABLED=false`，派生 spread/depth/imbalance/seal amount 保持 NULL。

任务只允许通过开关启用或停用；执行时间和采样频率由代码目录固定：

```text
AUCTION_COLLECTION_ENABLED=true
PYSNOWBALL_TOKEN=<server-only-xueqiu-cookie>
```

Token 失效、上游限流或 Schema 变化会使相应股票记为轮次缺失；查询
`ingestion.ingestion_run.error_summary`、`audit.quality_result` 和 session 的
`failed_quotes`，不要在日志中打印 Cookie 或上游响应体。

进程崩溃后只恢复当前及未来采样点；过去采样点记录为缺失，不允许补造。公开读取使用
authenticated PostgREST RPC `api_v1.query_auction_quotes`，请求必须带精确交易日或 session ID，
最多返回 5000 条。
