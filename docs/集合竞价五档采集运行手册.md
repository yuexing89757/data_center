# 集合竞价涨停池五档采集（已退役）

`opening-auction-limit-up-quotes` 已由 Issue #54 从 Worker 任务目录、APScheduler 注册和运行时
Provider 中移除。生产环境不再配置 `AUCTION_COLLECTION_ENABLED` 或 `PYSNOWBALL_TOKEN`，也不得
手工启动或补采该会话。

`auction_collection` 工作流编码、历史 Session/Round/Snapshot、Raw、ingestion lineage 和只读查询
继续保留，用于审计既有事实。陈旧历史会话仍由统一恢复流程最终化；该恢复能力不会创建新采集。

当前早盘采集由独立的 PYTDX 全市场任务承担：

- `call-auction-market-series`：2026-09-07 起 09:15:00–09:24:40 每 20 秒一轮，随后采集
  `09:24:53` 和 `09:25:20`，保存批次编码和完整五档；
- 末轮 `sample_seq=31`、`batch_code=092520` 同时供批量快照和一字涨跌停 API 读取；
- `call-auction-market-snapshot-daily` 已退役，Worker 不再在 09:25:30 重复采集。

全市场竞价序列明细先由 `call-auction-market-series-archive-daily` 每天 02:30 全字段幂等归档到
`realtime.call_auction_market_series_snapshot_history`。Worker 内部 `data-cleanup-daily` 每天 03:00
校验归档完整后清理在线表，只保留当前上海日期以前最近三个 `CN_A_SHARE` 交易日；历史表滚动保留
六个自然月。该任务不清理 Session、Round、Raw、lineage、质量记录或历史单次快照表，也不删除
月度分区；`.env` 只允许用两个任务的布尔开关启停。
